<h3 class="retro-font-last">Edit<br>
  <%= @article.title %></h3>

<div>
  <% if @article.photo.attached? %>
    <%= image_tag url_for(@article.photo), :class=> "edit-box" %>
  <% else %>
    <h3>No Photo</h3>
  <% end %>
</div>

<%= render partial: "form" %>
